<% moment = require 'moment/moment' %>

<span class="timestamp">
  <span class="relative"><%= moment(@timestamp).fromNow() %></span>
  <span class="absolute"><%= moment(@timestamp).format('LLL') %></span>
</span>
