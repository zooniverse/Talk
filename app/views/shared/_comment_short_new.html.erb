<!-- Box for adding a new short comment -->
<%= form_for [@dis, @comment] do |f| %>
	<p>Add a comment</p>
	<%= f.text_area :body, :rows => 2, :cols => 60 %>
	<%= hidden_field_tag('comment[author_id]', current_zooniverse_user.id)  %>
	<%= submit_tag 'Submit' %>	
<%- end %>