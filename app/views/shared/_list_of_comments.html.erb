<!-- Box for front page recent comments -->
<%= render :partial => "shared/comment_new" if current_zooniverse_user && @discussion %>
<div id="<%= id_of_box %>" class="panel">
	<% comments_list.each do |c| %>
	<% discussion = Discussion.find_by_id(c.discussion_id) %>
		<div class="recent-comment">
			<%= image_tag('default_user.png', :class => "recent-comment-user", :height => 40) %>
			<div class="comment-container">
				<div class="name"><%= link_to c.author.name, user_path(c.author.id) %> in <%= link_to truncate(discussion.subject, :length => 25), discussion_url_for(discussion) %></div>
				<div class="date">Posted <%= time_ago_in_words(c.created_at) %> ago</div>
				<div class="comment-body"><%= (markdown(c.body)).gsub(/<\/?[^>]*>/,"") %></div>
			</div>
			<div class="push"></div>
		</div>
	<% end %>
</div>